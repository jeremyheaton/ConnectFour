To run the program, clone the repo and open up the root directory of the project. 

From there run the command 
java -jar target\ConnectFour-0.0.1-SNAPSHOT.jar

To do a new build run
mvn clean install